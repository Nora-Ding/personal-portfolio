# Personal Portfolio

A modern, responsive personal portfolio site built with HTML, CSS, and JavaScript.

## Tech Stack

- HTML5
- CSS3 (custom properties, flexbox, grid)
- Vanilla JavaScript

## Setup

Open `index.html` in your browser, or serve with any static server:

```bash
npx serve .
```
